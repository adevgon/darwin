import React from 'react'
import { View, Text } from 'react-native';

const TestDetailScreen = ()=> {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Testing Detail Screen</Text>
        </View>
    );
}

export default TestDetailScreen;
